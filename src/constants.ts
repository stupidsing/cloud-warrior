export let prefix = 'npt-cloud';

export let dependersDirectory = '.warrior/dependers';

export let stateDirectory = `.warrior/states`;

export let getStateFilename = key => `${stateDirectory}/${key}`;
